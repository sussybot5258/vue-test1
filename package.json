{
  "name": "test1",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bun check && bun build-only",
    "preview": "bunx --bun vite preview",
    "check": "bunx --bun vue-tsc --noEmit",
    "build-only": "bunx --bun vite build --minify false"
  },
  "dependencies": {
    "vue": "^3.4.21",
    "vue-router": "4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "@types/bun": "^1.0.7",
    "@vitejs/plugin-vue-jsx": "^3.1.0",
    "typescript": "^5.2.2",
    "vite": "^5.1.6",
    "vue-tsc": "^1.8.27"
  }
}
